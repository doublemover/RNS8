# Performance Gain Completed Work

This archive holds Performance Gain Work Queue ranks that are no longer active
execution items. "Closed" means the lane's current evidence, benchmark,
schema, helper, or validation surface is complete enough to leave the active
queue. It does not necessarily mean a production speedup was promoted.

Active and partially advanced work remains in
[performance-gain-work-queue.md](performance-gain-work-queue.md). Durable
performance claims still live in [performance-wins.md](performance-wins.md),
[reviewed-local-evidence.md](reviewed-local-evidence.md),
[roadmap-status.md](roadmap-status.md), and the README
[current local performance snapshot](../README.md#exactness-and-performance).

## Completed And Closed Ranks

Current count: 55 closed/completed ranks.

## CDNA-Ready Infrastructure Notes

Rank 47 is now closed for Windows `gfx1100` exact-wide export-bound selector
implementation and local disposition. The resulting Direct-HIP route is narrow
and local: signed prefix18, four requested limbs, and large outputs use the
tree/CRT export path; compact-D2H, unsigned tree/CRT, and prefix20 fixed-export
variants remain experimental or deprioritized.

Rank 50 is now closed as the local bounded-i64 1024 hipBLASLt cache-maintenance
review gate. It proves the report machinery and full comparator/reuse-mode
coverage contract, not a new CDNA or README performance claim.

Ranks 60 and 74 remain repository-side control surfaces for a later CDNA
validation run: promotion-ledger/cache replacement history and K-block/tile-K
scenario/report metadata. They remain active evidence gates until real Linux
ROCm CDNA host captures and target-specific cache eligibility reports exist.
These notes are not counted as closed production speedups and do not imply
Instinct readiness.

Rank 59 is closed as non-routing shape-family shadow infrastructure:
recommendations can explain exact hits, nearest same-boundary representatives,
and rejected cross-boundary candidates, and promotion-ledger output can carry
that blocker metadata. It does not change AUTO routing, cache entries,
README claims, or CDNA readiness.

Rank 61 is closed as repository-side counter/resource audit infrastructure:
batch reports can join event medians, profiler counter exports, ISA summaries,
resource signals, bottleneck groups, and evidence-status diagnostics with
fixture coverage for present, missing, partial, and multi-capture cases. Real
target profiler exports are still required before any target-specific tuning
claim, cache change, or CDNA readiness statement.

Rank 62 is closed as repository-side target-validation gate infrastructure:
reports now separate OS, target id, target family, toolchain version, device
instance, runtime/driver versions, accelerator availability, validation phases,
and cache eligibility, with fixtures for Windows `gfx1100`, Linux RDNA,
CDNA2 `gfx90a`, CDNA3 `gfx942`, CDNA4 `gfx950`, multi-GPU shard topology, and
unsupported targets. Real Linux ROCm/RDNA/CDNA host runs are still required for
platform claims.

Rank 57 is now closed for the repository-side Direct-HIP workspace arena
surface and local Windows `gfx1100` measured-repeat allocation evidence. Future
arena-related speedup work should flow through the active residency, reuse,
chain, grouped, or export-selector lanes instead of keeping a standalone arena
rank open.

The June 6 pending-validation follow-up adds `tools/pending_validation.py` as a
target-generic command-planning, review-indexing, post-report, and summary core;
`tools/gfx1100_pending_validation.py` is now the Windows `gfx1100`
release-control wrapper.
`tools/multigpu_shard_report.py` for independent CDNA shard aggregation.
`scripts/cdna_env_probe.sh` now emits physical-device topology records and
`scripts/cdna_multigpu_smoke.sh` records shard status by physical GPU id, so
partial lists such as `4,5,6,7` do not inherit rank-zero metadata. This is
readiness/reporting infrastructure only; no closed-rank count, installed cache
entry, README claim, or CDNA performance status changes from it.

| Rank | Work Item | Why Now | Evidence Gate | Disposition Rule |
|---:|---|---|---|---|
| 1 | Closed analysis lane: evidence database and roofline priority summary | The repo needed a compact analysis layer that ranks where corpus time is going | `tools/evidence_database.py` validates schema-v4 captures, can skip/report stale temp captures, joins review/scenario/ISA inputs, writes ignored JSON/CSV/Markdown, and ranks global plus GPU-only roofline priority groups by measured bottleneck time | Closed as planning infrastructure; continue using it to choose release A/B work, not as a speedup claim |
| 2 | Closed scenario-surface lane: benchmark corpus for repeated, small, skinny, chain, finite, exact-wide, wrap64, CAS, and FHE-proxy workloads | Shape-only GEMM comparisons are no longer enough to choose real performance work | Scenario mode now covers repeated-B, reuse-contract A/B/A+B, small one-shot, many-small, skinny/GEMV, RNS-chain, finite distributions, finite generic moduli, exact-wide export, wrap64 carry/large probes, CAS/FHE proxies, native/vector-to-RNS, fused/pack/fusion, generated-prefix, adaptive, large-shape, and layout-search families | Closed as corpus infrastructure; keep expanding only for newly discovered workload classes with exact CPU checks and no unsupported product-scope implication |
| 3 | Completed 2048/4096 large-shape non-reuse matrix | The project needed release-size proof beyond 512/1024 to distinguish launch/export-bound paths from throughput-bound paths | 2048 bounded i64/u64, finite hot-modulus, exact-wide signed/unsigned, and strict wrap64 are release-reviewed; 4096 bounded i64/u64, finite hot-modulus, exact-wide signed/unsigned, and strict wrap64 have CPU/reference-backed release evidence where required. The promotion-ledger closeout under `temp/perf-work-queue/large-4096-cache-closeout-current/` installed eight eligible 4096 non-reuse cache entries and verified the installed 39-entry local cache with zero blockers | Closed for the large non-reuse matrix: keep repeated-B under reuse workload ranks, keep strict wrap64 as Direct-HIP correctness/tuning work, and keep Linux/Instinct promotion under target-validation ranks |
| 4 | Completed current-v2 bounded-u64 rerun | Stale vector-leadership claims needed replacement after Direct-HIP and selector changes | Current-code release review covered square 64/128/512/1024 plus selected skinny cases | Closed for current claim refresh; follow-on tuning should target CK 512, hipBLASLt 256x1x4096, and Direct-HIP-favored 128/1024 cases |
| 5 | Completed current-v2 adaptive bounded rerun | The adaptive bounded-i64 winner used an older rocWMMA tiled-v1 identity; current-v2 review needed real CPU, Direct HIP, runtime vector, CK, and rocWMMA evidence | `adaptive-bands` release review with seed `20260604`, three warmups, nine repeats, schema-valid captures, required GPU events, and corrected same-contract grouping covered bounded-i64 256/1024 plus bounded-u64 512x1024 | Closed for current claim refresh: Direct HIP wins all reviewed adaptive-bands groups, no accelerator cache is promoted, and old rocWMMA v1 evidence is historical |
| 6 | Completed/deprioritized bounded-i64 Direct-HIP 512 tuning pass | The reviewed 512 winner is Direct HIP, so local gains had to come from correctness-baseline overhead, not accelerator routing | The branch tested selected-prefix grouped launch routing, transient fixed-prefix i8 input routing, alternate tile shapes, and bounded export no-sync. All were rejected for default resident 512 routing after same-contract evidence failed the end-to-end gate. The paired export-sync A/B under `temp/perf-work-queue/direct-hip-512-rank6-paired-export-sync-baseline/` and `temp/perf-work-queue/direct-hip-512-rank6-paired-export-nosync-candidate/` was schema/event-valid but the no-sync candidate lost badly: 2321 us versus 1655 us median end-to-end and 467.55 us versus 234.48 us `crt_export` event median. The earlier selected-prefix grouped experiment under `temp/perf-work-queue/direct-hip-512-rank6-selected-prefix-grouped/` was also schema/event-valid but slower end-to-end than the current route | Closed as no-new-route evidence: keep the existing reviewed 512 Direct-HIP route and README claim, retain the public prefix-9 one-shot colpair win under prefix-fusion work, and send any future 512 work through active resident-layout, pack/export, and reconstruction ranks with a stricter paired A/B gate |
| 7 | Folded duplicate: bounded-i64 hipBLASLt 1024 tuning | The active queue had two rows for the same narrow hipBLASLt 1024 cache-protection work | Rank 50 now owns and closes the stronger A/B gate: current v2, scratch/reducer variants, reuse variants, export variants, and setup-inclusive Direct-HIP baselines | Closed as a duplicate control-panel row; no performance claim changes |
| 8 | Completed many-small persistent/grouped workload matrix | The grouped path now has the full release-control matrix named by the active queue instead of isolated smokes | `tools/many_small_grouped_report.py` now runs directly over normal benchmark-sweep roots and the fresh combined report under `temp/rank8-many-small-closeout/` classifies all nine required rows as candidate wins with no missing baselines: exact-wide signed/unsigned 64 and 128 group32, bounded-i64/u64 64 group32, bounded-i64 128 group64, bounded-u64 128x1x1024 group128, and finite-ring u8 mod251 64 group32. Every grouped row has release settings, required GPU events, a valid grouped task descriptor, fastest-independent comparison, same-task-count Direct-HIP hostbatch comparison, matching hostbatch task count, and hostbatch checksum parity. Durable summaries already live in `docs/performance-wins.md` and `docs/reviewed-local-evidence.md` | Closed as benchmark-owned same-shape grouped workload evidence. No public generic grouped ABI, grouped pack/export API, AUTO/cache promotion, README headline expansion, Linux/CDNA claim, or multi-GPU claim is made; future grouped work continues under active RNS-chain, resident-lifetime, adaptive grouping, and target-validation lanes |
| 9 | Completed RNS-chain final-output matrix | The active queue needed release-size proof that `RNS GEMM -> RNS GEMM -> final export` beats independent export/repack controls beyond the original 128/256 smokes | The rank-9 broader release sweep under `temp/rank9-rns-chain-broader-release/` adds 24 schema-v4 captures with seed `20260606`, three warmups, nine repeats, CPU final-output baselines, Direct-HIP resident-chain rows, and same-backend Direct-HIP independent export/repack controls for bounded-i64/u64 and exact-wide signed/unsigned at 512 and 1024. `tools/rns_chain_report.py` reports eight candidate final-output chain wins, zero missing baselines, zero experimental rows, zero deprioritized rows, and required GPU events for all Direct-HIP captures. The report also now ignores benchmark-sweep sidecars and normalizes backend-specific export selector keys so CPU and GPU same-output contracts group correctly | Closed as benchmark-owned final-output chain evidence only. No public lazy-output API, AUTO/cache route, README headline claim, Linux/CDNA claim, or reuse/prepack promotion is made; public lifetime and exact-wide output API semantics continue under ranks 46, 43, and 44 |
| 10 | Completed Direct-HIP prefix-9/prefix-20 fusion closeout | Prefix-specific work had multiple scattered implementation wins and rejected routes that needed one rank-level disposition | `tools/direct_hip_prefix_fusion_report.py` now summarizes the rank-10 evidence surface. The closeout under `temp/rank10-prefix-fusion-closeout-20260606/` has two current schema-valid/event-valid prefix-9 public one-shot colpair candidates, two legacy v1 before-captures, one schema-valid resident selected-prefix colpair rejection, and prefix-20 fixed-export selector rows imported from the rank-48 selector report. The report classifies two public one-shot colpair wins: bounded-i64 512 at 3048 us versus 9368 us median, `3.07x`, and bounded-u64 512 at 2841 us versus 4353 us median, `1.53x`. It also deprioritizes the resident selected-prefix colpair path at 4010 us versus 2434 us median and keeps prefix-20 fixed export experimental | Closed as classified implementation evidence. Keep the public one-shot colpair route for Direct-HIP fixed-prefix 512+ bounded i64/u64; keep resident colpair and prefix-20 fixed export out of default/resident/AUTO/cache routing unless future same-contract end-to-end evidence beats current grouped/generic/export paths |
| 11 | Folded exact-wide export specialization lane | The active row repeated work that later closed under the export-selector and exact-wide export-bound ranks | Ranks 47 and 48 now cover the current Windows `gfx1100` exact-wide export specialization surface: signed/unsigned 64, 128, 512, 1024, and 2048; fixed-limb selector metadata; compact-D2H candidates; status-elided exact-proof candidates; prefix20 fixed export; tree/CRT reconstruction; and same-contract three-limb/four-limb Direct-HIP A/B evidence. The June 6 closeout roots `temp/gfx1100-pending-validation-20260606/`, `temp/rank47-exact-wide-prefix18-fixed-20260606/`, `temp/rank47-tree-crt-20260606/`, `temp/rank47-tree-crt-followup-20260606/`, and `temp/rank47-tree-crt-default-route-final-20260606/` are schema-valid, event-valid where GPU events are required, and recorded in `docs/performance-wins.md` plus `docs/reviewed-local-evidence.md` | Closed as covered/folded exact-wide export work. Future export/reconstruction variants should reuse the rank-33 same-contract CPU/event/selector report gate instead of keeping this broad duplicate row active |
| 13 | Completed exact-wide baseline release matrix | Exact-wide small and large-shape baseline coverage is now current enough that the active row was mixing a completed victory lap with still-open export work | Current-v2 64/128 is release-reviewed; 2048 signed/unsigned and 4096 signed/unsigned have CPU/direct/accelerator release review with required events and installed eligible local cache entries where supported | Closed for baseline matrix coverage; reconstruction-backend classification is closed under rank 33, while exact-wide final-output chain work continues through the resident-chain/lifetime ranks |
| 14 | Completed finite-u8 2048 hot-modulus release matrix | 2048 GPU-only evidence needed CPU-backed release proof before any local AUTO claims | `large-release-validation` release-reviewed ring 251/255/256 and field 251 at 2048 with CPU, Direct HIP, hipBLASLt, CK, and rocWMMA comparators; the post-fix rerun after the hipBLASLt finite reducer-event fallback promotes hipBLASLt for all four hot 2048 contracts and refreshes the local cache with event-complete entries | Closed for 2048 hot-modulus promotion; 4096 hot-modulus promotion is closed separately under rank 3 |
| 15 | Folded duplicate: finite-u8 generic prime/composite coverage | Generic 512, generic 2048 ring, and generic 2048 field evidence now belong under the broader modulus-family map | Existing reviewed generic rows include local keys for selected 127/253 ring/field shapes with CPU/direct baselines and required accelerator events where promoted | Closed as duplicate/covered evidence; active successor work lives under rank 52 finite generic modulus family map |
| 16 | Closed helper lane: vector/native-to-RNS bridge | Native bounded output can now feed Direct-HIP RNS consumers instead of becoming a dead end | Device-to-device native-to-RNS kernels plus native-to-RNS and vector-to-RNS benchmark/schema/sweep coverage exist; release A/B and selector policy still need proof | Closed as bridge exposure; route only explicit conversion paths with stale-kernel schema rejection |
| 17 | Completed current skinny-GEMV selector refresh | Current-v2 bounded-u64 skinny evidence did not preserve the old vector-leading assumption | `skinny-gemv` release review covered bounded-i64 512x1x512, bounded-i64 256x1x4096, and bounded-u64 1024x1x1024 with CPU, Direct HIP, runtime vector ALU, hipBLASLt, CK, and rocWMMA; Direct HIP won all three groups and no cache entry was written | Closed for current claim refresh; reopen only for a cheaper native-input/reuse path that beats Direct HIP setup-inclusively |
| 18 | Completed reuse-contract release-matrix classification | The bounded A/B/A+B reuse work now has setup-inclusive classification; the active problem is public lifetime and routing policy, not re-proving the matrix exists | `tools/reuse_contract_report.py` classifies current bounded A/B/A+B release captures with setup-inclusive per-repeat time, same-backend and fastest-non-reuse speedups, break-even repeats, event availability, and source-identity metadata | Closed for release-matrix classification; active successor work lives under rank 43 reuse contract ledger and persistent matrix policy |
| 19 | Completed hipBLASLt A/B reuse conversion from benchmark win to explicit workload contract | hipBLASLt A/B reuse needed a fair setup-inclusive comparison against same-backend and fastest non-reuse baselines | The `reuse-contract` release matrix has hipBLASLt stable-A, stable-B, and stable-A+B captures at bounded i64/u64 1024 and 2048 with CPU, Direct HIP, runtime vector, CK, and rocWMMA baselines. 2048 stable-A and stable-A+B are explicit workload candidates for bounded i64 and u64; 1024 stable-A and stable-A+B are deprioritized; bounded-u64 2048 stable-A+B is strongest at 9198 us setup-inclusive per repeat, 2.35x faster than the same-run fastest non-reuse baseline | Closed for contract classification; promote only explicit reusable-input workloads with visible setup, source lifetime, stale-input rejection, and repeat-count break-even policy, never as a one-shot AUTO replacement |
| 20 | Completed Direct-HIP reuse-A/reuse-B expansion beyond uniform-small bounded cases | Direct-HIP reuse needed current evidence outside the original uniform-small bounded slices | The rank-20 release matrix under `temp/rank20-direct-hip-reuse-expansion-release-20260606/` produced 70 schema-v4 captures across adaptive bounded-u64 colpair reuse-A/B, finite-u8 native-A/reuse-B, exact-wide residue-current chain reuse-B, and strict wrap64 reuse-packed-inputs profiles. The regenerated sweep review has 26 release-satisfied groups, zero missing required baselines after explicit reuse-evidence groups delegate setup-inclusive baseline matching to `tools/direct_hip_reuse_expansion_report.py`, and zero duplicate backend records. The Direct-HIP reuse report classifies 14 Direct-HIP reuse comparisons: two strict wrap64 A+B reuse wins at 512 and 1024, twelve deprioritized adaptive bounded/finite/exact-wide rows, zero experimental rows, zero missing baselines, required GPU events for all GPU captures, and 14/14 same-workload-family matches | Closed as per-profile classification, not broad Direct-HIP reuse promotion. Keep strict wrap64 reuse-packed inputs as local Windows `gfx1100` explicit workload evidence; keep adaptive bounded-u64, finite native-A/reuse-B, and exact-wide chain reuse-B deprioritized at the 9-repeat gate unless a future repeat-count/lifetime contract changes the setup economics |
| 21 | Completed/deprioritized bound-discovery proof-mask setup-inclusive release matrix | Proof masks reduced scan cost but needed broader end-to-end proof | `bound-discovery` release matrix has 51 schema-valid captures, nine reviewed groups, required GPU events for all non-CPU captures, and `tools/bound_discovery_report.py` compares static global bounds, global input-scan, and proof-mask per-tile modes with scan setup cost included | Closed as no-promotion evidence: all 33 input-scan/proof-mask candidates lose the setup-inclusive gate versus same-backend or fastest static baselines; do not route discovery/proof modes until a cheaper prepass or persistent bound contract changes that result |
| 22 | Completed/deprioritized zero-tile and zero-row/column skip expansion beyond Direct-HIP | The active queue needed a clear answer on whether proof-mask execution skips should expand to accelerator backends after scan-derived proof masks lost setup-inclusively | `tools/zero_skip_expansion_report.py` now classifies backend capability and capture evidence for zero-output tiles versus zero row/column product masks. The closeout report under `temp/rank22-zero-skip-closeout-20260606/` reuses the 51 schema-valid bound-discovery captures: 15 captures request row/column proof products, 15 request zero-output tiles, three Direct-HIP row/column skip captures are present but scan-derived, six CK/rocWMMA rows are correct full-tile fallbacks rather than row/column product skips, six CPU/vector rows are unsupported for the scheduled proof-mask contract, and no capture is caller-provided or naturally sparse proof metadata | Closed as no-expansion evidence: Direct-HIP remains the only backend with row/column product mask execution, CK/rocWMMA keep whole zero-output tile handling only, hipBLASLt remains unsupported for scheduled/adaptive tiled RNS plans, and scan-derived proof masks should not be chased as default routing. Reopen only for caller-provided proof metadata or naturally sparse workloads with same-contract CPU, Direct-HIP, accelerator correctness, required events, and setup-inclusive wins |
| 23 | Closed helper lane: generated prefix-specific reducers for bounded prefixes 1..9 | PR #10 adds fixed-prefix reducer identity, dispatch, and ISA-gate surfaces | Release A/B still needs prefix-specific end-to-end proof against generic reducers | Closed as infrastructure; reopen only for measured prefix-specific speedup work |
| 24 | Closed generated reducer/epilogue registry | Reducer and epilogue identity drift needed one metadata-backed source before more backend variants add names, cache keys, and ISA report hooks | `metadata/epilogues.yaml` now declares backend epilogue modes, generated reducer identities, finite-modulus identity patterns, and reducer-family records that reference known selected kernels, generated helpers, and ISA evidence. `tools/metadata_registry.py` validates those references, emits Python/C++ constants, and schema helper metadata now validates `timing_metadata.generated_reducer_identity` through generated registry constants instead of duplicated local regexes. Tests cover stale reducer family identities, stale reducer kernel references, stale generic names, and undeclared prefix reducers | Closed as registry/schema infrastructure. No hot runtime path parses the registry, no default route changes, no cache entry changes, and no performance claim is added; future reducer variants must update the registry before schema/cache/evidence tooling accepts their metadata |
| 25 | Closed helper lane: residue-channel fusion experiments | PR #10 adds benchmark-only residue-channel fusion metadata and stale-schema rejection | Microbenchmarks and release captures still need to show fewer launches/materializations | Closed as exposure; continue only if fusion wins end-to-end after pack/export effects |
| 26 | Closed helper lane: multi-modulus pack experiments | PR #10 adds pack-layout and residue-group metadata for comparison keys | Same-contract pack event comparisons still need required correctness checks | Closed as metadata surface; keep variants only if pack savings survive GEMM/export timing |
| 27 | Closed helper lane: fused pack+GEMM for small one-shot bounded and finite workloads | PR #10 adds comparison surfaces for one-shot/transient fused-path experiments | Release captures still need 64/128 bounded and finite one-shot proof | Closed as benchmark surface; promote only if fused path beats CPU, Direct HIP, and current accelerator winner |
| 28 | Closed scenario surface: end-to-end layout search across RNS, finite, exact-wide, and wrap64 | Layout decisions now affect pack, GEMM, reducer, export, and reuse together | `layout-search` emits layout-metadata captures for bounded RNS final export, RNS-next-op, exact-wide prefix-20, finite-u8 ring/field, and strict wrap64 byte-limb paths | Closed as benchmark surface; keep layout variants only after complete same-contract release evidence and event attribution |
| 29 | Folded duplicate: persistent/grouped scheduler for adaptive prefix groups | The active queue had overlapping adaptive grouped scheduler rows | Active rank 54 owns the implementation gate for compact per-prefix/per-tile grouping, per-group events, and CPU/direct baselines | Closed as a duplicate control-panel row; grouped adaptive execution remains active under rank 54 |
| 30 | Completed HIP Graph replay release-size validation | The existing graph lane was only smoke-sized; rank 30 needed release-size same-contract A/B proof against ordinary Direct-HIP resident chains | `hip-graph-replay` now covers bounded-i64, bounded-u64, exact-wide signed, and exact-wide unsigned residue-current chain3 workflows at 512 and 1024. The Windows `gfx1100` release sweep under `temp/rank30-hip-graph-replay-release-20260606/` produced 16 schema-v4 captures, eight release-satisfied graph/baseline groups, zero missing required baselines, and zero duplicate backend records. Ordinary non-graph baselines have required GPU events; graph captures are schema-valid wall-clock graph-launch timing by contract. `tools/hip_graph_replay_report.py` compares setup-inclusive per-repeat time with A/B prepack plus graph capture/instantiate amortized and reports eight checksum-matched comparisons: four candidate workload wins and four deprioritized rows | Closed as benchmark-only graph replay classification. Keep bounded-i64 1024, bounded-u64 512/1024, and exact-wide signed 512 as local Windows `gfx1100` graph workload wins; keep bounded-i64 512, exact-wide signed 1024, and exact-wide unsigned 512/1024 deprioritized. No AUTO/cache entry, README headline claim, public async API, default route, or Linux/CDNA claim changes |
| 31 | Closed benchmark/evidence lane: host API batching for many-small workloads | Some workloads may be too dynamic for graph capture | `rns8-bench --host-api-batch-size` now runs same-shape independent resident tasks through one shared plan, one A/B/C matrix triplet and workspace per task, aggregate pack/GEMM/export timing, combined output checksums, schema-v4 host-batch metadata, sweep scenarios for bounded, finite, skinny, and exact-wide many-small cases, distinct `*-hostbatch` review identities, `tools/host_api_batch_report.py`, and a same-commit 20-capture release comparison against independent-call baselines | Closed as benchmark/evidence plumbing; only Direct-HIP exact-wide signed 64 hostbatch32 currently wins the full workload gate, and no AUTO/public route changes are made |
| 32 | Closed helper lane: next-op and lazy-export metadata | PR #10 adds requested next-op and output-domain planning metadata to schema-v4 captures | Chain/lazy-export scenarios still need release proof with exact final CPU comparison | Closed as metadata; keep advisory until public API semantics are clear |
| 33 | Closed reconstruction backend variants for GPU CRT/export | Export/status/reconstruction timing needed one rank-level A/B classifier after ranks 47 and 48 added selector identity and candidate captures | `tools/reconstruction_export_report.py` now classifies same-target setup-inclusive A/B pairs for compact D2H/host-scatter, status-elided exact-proof export, prefix20 fixed export, and tree/CRT reconstruction. The closeout under `temp/rank33-reconstruction-export-closeout-20260606/` consumes 14 schema-valid Windows `gfx1100` rank-48 selector captures and release review reports, validates seven hip-direct captures with required GPU events, and reports eight baseline/candidate comparisons with no blockers: compact hip-direct loses at `0.991x`, status-elided hip-direct loses at `0.960x`, prefix20 fixed hip-direct is a narrow experimental win at `1.020x` end-to-end while export events lose, and tree/CRT hip-direct is a narrow experimental win at `1.048x` end-to-end with `1.042x` export-event improvement | Closed as local Windows `gfx1100` selector/reconstruction classification. Keep tree/CRT and prefix20 fixed export experimental/non-default until broader same-target evidence and selector policy promote them; do not update README/cache/Linux/CDNA claims from this report |
| 34 | Closed helper lane: explicit padded/contiguous output policy | PR #10 records output policy and status/export handling in schema-v4 captures | Release captures still need padded versus contiguous A/B with event-visible D2H/status phases | Closed as metadata; default-enable only per semantic/layout where repeated evidence wins |
| 35 | Closed helper lane: CPU/GPU hybrid AUTO selector explanations | PR #10 reports AUTO cache hits, fallbacks, and rejection reasons for benchmark/inspect tooling | Selector behavior still needs reviewed cache evidence before routing changes | Closed as explanation surface; promote selector behavior only without weakening evidence gates |
| 36 | Closed AUTO cache shape-family recommendation gate | Rank 59 provided advisory workload-query recommendations, but the active cache/AUTO lane still needed a gate proving family recommendations stay layered above exact-shape cache entries without changing runtime routing | `tools/auto_shape_family_gate.py` now consumes reviewed cache JSON plus one or more `shape_family_shadow_report.py` outputs, verifies `src/core/autotune_cache.cpp` still routes through exact cache lookup with no runtime shape-family lookup, validates the shadow schema/policy/boundary fields, requires every recommendation to remain `runtime_routing_allowed=false` and `promotion_eligible=false`, requires exact hits to stay owned by current exact AUTO cache entries, requires non-exact representatives to carry `exact_query_not_reviewed` and same-target/layout/contract review blockers, requires missing same-boundary queries to expose boundary rejection blockers, and checks referenced basis keys exist in the reviewed cache. The closeout under `temp/rank36-auto-shape-family-gate-20260606/` uses the current shape-family cache fixture and regenerated shadow report with three queries: one exact 512 cache hit, one same-boundary 768 advisory CK representative, and one rejected cross-output-contract 768 query; the gate completes with zero blockers | Closed as exact-cache/AUTO safety infrastructure only. Shape-family recommendations remain advisory and non-routing; no installed cache entry, AUTO route, README speed claim, Linux/RDNA, or CDNA performance claim changes |
| 37 | Closed helper lane: device plan cache and workspace arena evidence | PR #10 adds plan/workspace fingerprints and post-warmup allocation evidence | Benchmarks still need to separate planning, allocation, pack, GEMM, and export for real cache work | Closed as evidence surface; promote only when cache identity includes plan, target, backend, and source versions |
| 38 | Closed verification-cost reduction gate for repeated exact workloads | Verification-amortized proxy scenarios needed a mechanical proof that reused CPU/reference setup never weakens final exact checking or cache promotion gates | `tools/verification_amortization_report.py` now consumes schema-valid captures plus `review_report.json` sidecars, selects captures with `verification_amortization.enabled=true`, and requires final exact comparison to remain required, the comparison status to be explicit, the amortized row to be non-promotable and cache-ineligible, the scenario scope to be tooling/proxy-only, the review group to include CPU/reference coverage, release review CPU/reference coverage when the group is release-reviewed, no duplicate backend records, and no missing CPU required baseline. The FHE/lattice key-switch proxy scenario now includes the CPU backend, and `tools/test_benchmark_sweep.py` rejects any future scenario that requests verification amortization without a CPU backend. The closeout under `temp/rank38-verification-amortization-closeout-20260606/` validates one schema-v4 amortized proxy capture with CPU-reference, Direct-HIP, and hipBLASLt review candidates; the report completes with one ready row and zero blockers | Closed as tooling-only exact-verification safety infrastructure. No probabilistic correctness mode, default API change, cache entry, AUTO route, README speed claim, Linux/RDNA, or CDNA performance claim changes |
| 39 | Closed error-detecting exact fast-path metadata gate | Probabilistic or checked fast paths can be useful research lanes only if every capture explicitly declares verification basis, false-negative policy, and non-default semantics | `rns8-bench` and `tools/benchmark_sweep.py` now carry `error_detection_policy` metadata into captures and scenario manifests. Schema validation requires enabled error-detection captures to be research-only, non-promotable, cache-ineligible, non-routing, default-exact-API-preserving, final-exact-comparison-required, and false-negative-policy declared; probabilistic product-check captures must also record positive verification rounds and RNG seed status. `tools/error_detection_policy_report.py` consumes schema-valid captures plus `review_report.json` sidecars and requires CPU/reference coverage, no duplicate backend records, no missing CPU required baseline, no review/cache promotion, and research-only scenario scope. The `error-detecting-fast-path` scenario family schedules CPU, Direct-HIP, and CK research rows, and the closeout under `temp/rank39-error-detection-policy-closeout-20260606/` validates one schema-v4 Freivalds-style proxy row with CPU-reference, Direct-HIP, and CK review candidates and zero blockers | Closed as research/tooling safety infrastructure only. Default exact APIs remain deterministic; no probabilistic correctness mode, AUTO route, installed cache entry, README speed claim, Linux/RDNA, CDNA, or production performance claim changes |
| 40 | Closed helper lane: hardware-counter/RGA ingestion into evidence reports | PR #10 adds counter report tooling and ISA/capture cross-links | Optional counter/ISA summaries can now attach to reviewed captures | Closed as tooling; use as explanation evidence only, never as a replacement for timing and correctness gates |
| 41 | Closed helper lane: architecture-specific kernel namespaces and target-keyed variants | PR #10 adds target-id, namespace, configured target, runtime version, and review grouping metadata | Target-specific cache entries still need real host evidence | Closed as schema/tooling; promote non-`gfx1100` only after evidence on that target |
| 42 | Folded duplicate: Linux/Instinct/toolchain matrix gates | The active queue had two non-Windows platform validation rows | Active rank 62 owns the broader Linux/RDNA/CDNA validation matrix and target-report gate, including `tools/target_validation_report.py` and real-host release evidence requirements | Closed as a duplicate control-panel row; Windows `gfx1100` evidence remains local |
| 43 | Completed reuse contract ledger and persistent matrix policy gate | Reuse/prepack wins needed a current ledger that proves setup-inclusive value, same-workload grouping, stale-source rejection, and selector eligibility instead of only same-backend mechanism speedups | The June 6 Windows `gfx1100` release rerun under `temp/rank43-reuse-contract-release-20260606/` produced 96 schema-v4 captures across 16 release groups with seed `20260606`, three warmups, nine repeats, CPU, Direct HIP, runtime vector ALU, hipBLASLt, CK, and rocWMMA comparators where supported. `tools/reuse_contract_report.py` now consumes current `reuse_contract` metadata, ignores reuse-only lifetime fields when grouping same workload baselines, normalizes backend-specific export selector keys, reports stale-source rejection and selector policy, and classifies 11/72 reuse rows as explicit-workload selector-ready, 49 as deprioritized, 12 as experimental, and zero as missing a baseline. The sweep review has no missing required baselines, duplicate backends, incompatible compiler/GPU/git/HIP metadata, or release-setting gaps; required GPU events pass for all non-CPU captures | Closed as explicit reusable-input workload policy and evidence only. No AUTO/cache entry, README headline claim, Linux/CDNA claim, or default one-shot route changes; the general public resident lifetime inspection contract is closed separately under rank 44 |
| 44 | Closed persistent resident matrix lifetime contract | Persistent RNS/native matrices needed a public and benchmark-visible contract for A/B/C role binding, source versions, workspace identity, and output currentness before residency, reuse, chain, or grouped routes can safely build on them | `rns8_get_resident_lifetime_info` now reports a read-only resident lifetime contract for a matrix role with optional plan/workspace binding: source version and validity, current output domain, host/device currentness, device residency, plan fingerprint, workspace fingerprint, role-specific A/B/C shape match, workspace identity/schedule/backend match, device-id match, next-op eligibility, and deterministic mismatch policy. The C++ wrapper exposes matrix-only, matrix+plan, and matrix+plan+workspace helpers. `rns8-bench` resident-lifetime metadata now names the contract API, A/B/C shape policy, workspace binding policy, and output-currentness policy. CPU unit coverage proves matching A/workspace contracts, wrong-role rejection, mismatched-workspace rejection, zero-version C output reporting, invalid ABI rejection, and wrapper coverage | Closed as public/benchmark lifetime-contract infrastructure only. No new performance claim, AUTO route, installed cache entry, README speed row, Linux/CDNA claim, or default residency optimization changes; follow-on speed work continues under active reuse, graph, reconstruction, adaptive grouping, streaming, and layout ranks |
| 45 | Completed grouped-dispatch ABI/lifetime and bucket-contract gate | Grouped dispatch needed its public contract, benchmark-owned lifetime limits, and backend descriptor bucketing made mechanically visible before many-small follow-up work could be judged cleanly | Public read-only contract introspection through `rns8_get_grouped_dispatch_contract_info` now exposes caller-owned unique task triplets, call-return lifetime, explicit caller pack/export phases, same-shape resident grouped GEMM availability, generic public bucketing disabled, and AUTO routing disabled. The internal Direct-HIP grouped descriptor layer now enforces same-contract shape buckets, same target device, validated per-bucket descriptors, unique matrix/workspace ownership across buckets, contiguous task offsets, output-domain policy, and descriptor lifetime. Registry, schema, and `many_small_grouped_report.py` now understand `same_contract_shape_buckets`. Existing Windows `gfx1100` release artifacts report nine grouped candidate wins with no missing baselines: five bounded/finite wins under `temp/perf-queue-grouped-broader-release/` and four exact-wide signed/unsigned wins under `temp/perf-queue-grouped-exact128-release/` | Closed as ABI/lifetime and descriptor-bucketing infrastructure plus local grouped evidence. Generic public grouped routing, public grouped pack/export, AUTO/cache promotion, README claims, Linux/CDNA validation, and further many-small tuning remain outside this closed rank and continue under active grouped, residency, chain, and target-validation lanes |
| 46 | Completed exact-wide residue-current/final-output chain matrix and API draft | Lazy exact-wide residue-current chains needed paired final-output proof and explicit lifetime semantics before they could leave the active queue | `benchmarks/scenarios/exact_wide_output_chain_broader.json` adds exact-wide signed/unsigned 512 and 1024 residue-current chain3 and chain4 release controls plus matching chain4 final-output CPU/Direct-HIP pairs. Combined with rank-9 chain3 final-output captures, `tools/exact_wide_chain_report.py` reports eight paired residue-current/final-output rows, eight ready pairs, zero missing residue-current captures, zero missing final-output captures, zero missing CPU baselines, and required GPU events for every Direct-HIP capture. The draft resident-output API document now spells out benchmark-owned residue-current lifetime rules: same context/device/stream owner, same semantic/shape/prefix/output contract, monotonic source version, no host-current assumption before final export, no cross-workspace reuse, and final checksum/export as the only host-observable boundary | Closed as benchmark-owned exact-wide chain evidence and API design draft only. No public resident-output handle, AUTO/cache route, README headline claim, or Linux/CDNA claim is made; production/public lifetime implementation continues under ranks 43 and 44 |
| 47 | Completed export-bound exact-wide optimization and limb variants | The branch needed to turn export-selector metadata and scattered exact-wide candidate captures into concrete route decisions instead of leaving rank 47 as an open-ended research bucket | Exact-wide export scenarios now cover signed/unsigned 64, 128, 512, 1024, and 2048 plus fixed-limb, compact-D2H, status-elision, prefix20 fixed-export, and tree/CRT selector candidates. The June 6 Windows `gfx1100` closeout has 46 exact-wide-export rows and 64 export-bound-limb-variant rows in `temp/gfx1100-pending-validation-20260606/`, with schema-valid captures, zero missing required baselines, zero duplicate backend records, and required GPU events for GPU rows where required. The focused rank47 roots validate compact-D2H as a loser, prefix20 fixed export as experimental, unsigned tree/CRT as not worth routing, and signed prefix18 tree/CRT as a narrow Direct-HIP default route for four-limb large outputs only. Final default-route captures under `temp/rank47-tree-crt-default-route-final-20260606/` show 1024 signed at 14341 us versus 15032 us fixed-prefix Garner and 2048 signed at 93263 us versus 99186 us fixed-prefix Garner, with roughly 2x export-event improvement | Closed for current Windows `gfx1100` route/disposition work. No README headline claim, installed cache entry, default AUTO cache promotion, or Linux/CDNA claim changes; future reconstruction/export work should use the rank-33 same-contract CPU, event, and selector report gate when it introduces a new variant |
| 48 | Closed export/reconstruction selector production gate | Exact-wide fixed-limb wins needed a real selector identity, review, and cache safety surface before follow-on export work could be judged cleanly | The selector surface now records semantic, backend, target, prefix, limb count, signedness, output layout, status policy, D2H policy, final-output/chain mode, selector key/policy, selected kernel, cache visibility, stale-entry reason, and status-elision reason in schema-visible metadata. Review grouping separates selector variants, `result_compare.py --allow-export-selector-diff` compares selector A/B captures without weakening semantic/shape/limb/layout/target checks, cache install rejects selector-review-only entries unless explicitly allowed, and promotion-ledger/cache summaries expose selector blockers. The June 6 Windows `gfx1100` closeout under `temp/rank48-selector-ab-20260606/` has schema-valid release A/B captures and required GPU events for compact D2H, status-elision, prefix-20 fixed export, and tree/CRT candidates. Median end-to-end dispositions versus Direct-HIP defaults were: compact signed 1024 `0.991x` keep experimental, tree/CRT signed 1024 `1.048x` promising follow-up, status-elided unsigned 1024 `0.960x` drop/deprioritize, and prefix20 fixed signed 1024 `1.020x` promising follow-up | Closed as selector review/cache-safety infrastructure and local `gfx1100` evidence. No README headline claim, installed cache entry, default AUTO route, or Linux/CDNA claim changes; rank 47 has since closed the current implementation/disposition follow-through |
| 49 | Completed budgeted 4096 release-reference gate | 4096 throughput classification needed required CPU/reference baselines without pretending GPU-only rows were promotional | `large-release-validation-4096-budgeted` now has bounded i64/u64 CPU, Direct HIP, runtime vector, hipBLASLt, CK, and rocWMMA rows; finite hot CPU, Direct HIP, hipBLASLt, CK, and rocWMMA rows; exact-wide signed/unsigned CPU, Direct HIP, hipBLASLt, CK, and rocWMMA rows; and strict wrap64 byte-limb plus Direct HIP rows. The current signed exact-wide closeout completed same-commit CPU/GPU evidence with hipBLASLt at 176943 us, Direct HIP at 639360 us, CPU reference at 113085000 us, and checksum `5508849193854467465`; strict wrap64 `wrap64-byte-limb` remains 102905000 us with checksum `13518998852724169131`; exact-wide unsigned completed with hipBLASLt at 162382 us, Direct HIP at 614116 us, CPU reference at 105462000 us, and checksum `9643325300233475427`. Required GPU events, pairwise result comparisons, candidate caches, and installed promotion-ledger reports are available under `temp/perf-work-queue/large-4096-release-reference-current/`, `temp/perf-work-queue/large-4096-unsigned-budgeted-release-current/`, `temp/perf-work-queue/large-4096-signed-cache-closeout-current/`, `temp/perf-work-queue/large-4096-signed-cpu-current/`, and `temp/perf-work-queue/large-4096-cache-closeout-current/` | Closed as validation and promotion evidence: historical timeout rows remain visible but are superseded by completed release-reference rows; eight eligible non-reuse 4096 entries are installed, while strict wrap64 and repeated-B remain outside AUTO cache promotion |
| 50 | Completed hipBLASLt bounded-i64 1024 A/B lane gate | The narrow bounded-i64 1024 hipBLASLt cache entry needed a current same-target gate that compares against every required local baseline and keeps reuse variants out of one-shot cache promotion | `tools/bounded_i64_1024_review.py` now canonicalizes CPU baselines, fixes pack-mode classification when `prepack_reuse_strategy=none`, requires CPU, Direct HIP, vector ALU, CK, and rocWMMA comparators, requires hipBLASLt per-repeat, reuse-A, reuse-B, and reuse-A+B pack-mode coverage, compares candidates against both Direct HIP and the fastest required comparator, consumes target-validation, variance, counter/resource, and promotion-ledger sidecars, emits `rank50_gate_complete`, and supports `--require-complete`. The self-test now covers full success, missing comparator, missing reuse mode, slow hipBLASLt drop/deprioritize, and unsupported-accelerator cases with schema-valid synthetic captures | Closed as cache-maintenance and review-control infrastructure. The existing Windows `gfx1100` hipBLASLt 1024 cache row can stay only when the complete same-target gate remains clean; reuse A/B/A+B rows stay explicit-workload/experimental unless a later production policy promotes them. No README headline, installed cache, Linux, or CDNA claim changes from this closeout |
| 52 | Completed finite generic modulus family map | Generic finite-u8 had hot-modulus wins and scattered generic rows, but no current shape/modulus family map | The June 6 Windows `gfx1100` release sweep under `temp/rank52-finite-modulus-map-release-20260606/` produced 200 schema-v4 captures covering finite ring moduli 127, 241, 243, 251, 253, 255, and 256 plus finite field moduli 127, 241, and 251 at 128, 512, 1024, and 2048 across CPU, Direct HIP, hipBLASLt, CK, and rocWMMA. `tools/finite_modulus_map_report.py --require-complete-map` reports 40/40 ready release groups, zero missing expected groups, zero missing backends, zero missing GPU events, and zero non-release-ready groups. The generic sweep review has 40 release-satisfied groups, no missing required baselines, no duplicate backend records, and the scenario scope now blocks autotune promotion as `non_promoting_modulus_map`. Winner distribution is CPU reference for all 128 rows, mixed Direct HIP/CK/rocWMMA/hipBLASLt at 512 and 1024, and hipBLASLt for all 2048 rows | Closed as local Windows `gfx1100` non-promoting modulus-family evidence. No README headline claim, installed cache entry, default AUTO route, or Linux/CDNA claim changes; future finite routing still needs exact semantic/modulus/shape/target cache review and variance gates |
| 57 | Closed Direct-HIP workspace arena allocation gate | The branch needed to prove workspace-owned buffers were not allocating/freeing inside measured repeats before arena evidence could be used in future residency/reuse decisions | The Direct-HIP arena surface ties scratch/staging/grouped/reuse buffers to plan, target, stream/device, semantic, prefix, output policy, and backend identity. The June 6 `gfx1100` closeout reran 13 resident-lifetime arena captures under `temp/gfx1100-pending-validation-20260606/`; every row in `workspace-arena/resident-workspace-report.json` reports measured-repeat allocation delta `{allocate_calls: 0, free_calls: 0, allocated_bytes: 0}` after the benchmark lanes started recording the repeat snapshot before final checksum export and teardown | Closed as infrastructure/evidence, not a speedup claim. Keep future arena optimization under active residency, reuse, chain, grouped-dispatch, or export-selector lanes with setup-inclusive timing proof |
| 59 | Closed advanced shape-family AUTO shadow mode | Exact-shape reviewed cache keys are too narrow for practical workloads, but broad shape-family routing would be unsafe without strict target/layout/contract boundaries | `tools/shape_family_shadow_report.py` now emits non-routing workload-query recommendations with exact-hit, same-boundary representative, and missing-same-boundary statuses; boundary fields include target, target family, semantic, signedness, finite modulus, layout, output contract, export selector, and limb count. Rejected same-family candidates now report explicit boundary mismatch blockers, and `tools/promotion_ledger.py --shape-family-shadow-report` surfaces the recommendations as blocking metadata on matching cache-basis rows | Closed as explanatory and promotion-control infrastructure only. AUTO routing remains exact-cache based, no installed cache entries or README claims change, and real shape-family routing remains active under rank 36 until same-target/layout/contract review can prove it safely |
| 61 | Closed counter-driven occupancy/resource audit batch | Event timing identifies large phases, but kernel work needs counter and resource evidence to explain occupancy, memory, stores, LDS, waits, or instruction mix | `tools/gpu_counter_report.py --batch` now validates captures, accepts optional per-capture attachment manifests, joins HIP event medians, JSON/CSV profiler counter exports, ISA/resource summaries, evidence-database bottleneck and roofline groups, and emits per-capture plus batch JSON/Markdown. Batch rows now summarize VGPR, SGPR, LDS, scratch, occupancy, memory-pressure, wait/stall, store, matrix-instruction, and work-intensity signals, with evidence-status diagnostics for missing counters, missing/partial ISA, and missing event medians. Regression fixtures cover present counters, missing counters, partial ISA data, multi-capture batch output, and per-capture manifest attachment | Closed as explanation/reporting infrastructure only. Counter/resource findings prioritize kernel experiments; they never replace exact correctness, release timing, required GPU events, same-contract baselines, target validation, variance gates, or claim validation |
| 62 | Closed Linux/RDNA/CDNA validation matrix and target report gate | Windows `gfx1100` evidence must not satisfy Linux, RDNA4, CDNA, or Instinct readiness | `tools/target_validation_report.py` now emits per-OS/target/toolchain groups with target family/class/tier, supported-target status, device names, device indices, target cache keys, target instance ids, visible and node GPU counts, BDF/NUMA topology, runtime/driver versions, configured AMDGPU targets, accelerator availability, validation phase counts, cache eligibility, and coverage summaries. Cache eligibility is scoped to the matching OS/target/toolchain group, cross-target promotion is always false, unsupported targets are blocked, and fixtures cover Windows `gfx1100`, Linux RDNA, CDNA2 `gfx90a`, CDNA3 `gfx942`, CDNA4 `gfx950`, 4-GPU and 8-GPU shard metadata, partial profiler/RCCL readiness, and unsupported targets | Closed as validation-gate infrastructure only. Real supported-host build/CTest/smoke/release/profiler evidence is still required before any Linux, RDNA4, CDNA, Instinct, cache, or README claim |
| 70 | Completed release variance and golden performance regression gate | Narrow reuse/export/cache wins needed a mandatory repeatability gate instead of an advisory report that promotion passes could skip | `tools/perf_variance_report.py` already validates schema-v4 captures, groups same-contract reruns, derives required speedup margins, and runs in the golden regression suite. The closeout adds `promotion_ledger.py --require-variance-gate`, blocks ledger rows that lack matching variance entries, and makes `tools/install_autotune_cache.py` require a promotion ledger with a ready variance gate for every real cache write or replacement. Dry-runs remain available for cache JSON validation, but cache promotion cannot bypass variance evidence. Regression tests cover missing variance, variance-blocked narrow speedups, variance-ready installs, and no-ledger write rejection | Closed as promotion-control infrastructure only. No performance claim, README row, installed cache entry, or Linux/CDNA readiness change is made by this rank |
